{
    // $deleteOne

    //db.test.deleteOne(
    //  {
    //    _id: ObjectId("69a813e5b474b36529b57bfa"),
    //  },
    //);

    // Output:
    // The deleteOne method is used to delete a single document from a collection that matches the specified filter. In this case, the query will find the document with the specified _id and delete it from the test collection. If a document with the specified _id exists, it will be removed from the collection. If no document matches the filter, no changes will be made to the collection. The deleteOne method will only delete one document, even if multiple documents match the filter criteria.
}