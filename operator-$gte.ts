{
    // Using $gte operator - greater than or equal operator

    // db.test.find({id: {$gte: 5}})

    // Output:
    // By using the $gte operator, we can find documents where the id field is greater than or equal to 5. This will return all documents with an id value greater than or equal to 5.
}