{
  //Using $ne operator - not equal operator
  
  // db.test.find({gender: {$ne: "Male"}})


  //Output:
  //By using the $ne operator, we can find documents where the gender field is not equal to "Male". This will return all documents where the gender field has a value other than "Male".
}
