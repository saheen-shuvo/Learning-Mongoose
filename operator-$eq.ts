{
  //Using $eq operator - equal operator
//   db.test.find({ gender: { $eq: "Male" } });

  //Output:
  //By using this equal operator, we can find documents where the gender field is equal to "Male".  The output will be the same as using the direct equality operator without $eq, but it allows for more complex queries when needed.
}
