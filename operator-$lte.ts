{
    //By using $lte operator - less than or equal operator

    // db.test.find({id: {$lte: 5}})
    
    // Output:
    // By using the $lte operator, we can find documents where the id field is less than or equal to 5. This will return all documents with an id value less than or equal to 5.
}