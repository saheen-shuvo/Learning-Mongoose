{
  //By using the $lt operator, we can find documents where the id field is less than 5. This will return all documents with an id value less than 5.
  
  // db.test.find({id: {$lt: 5}})
  
  // Output:
  // By using the $lt operator, we can find documents where the id field is less than 5. This will return all documents with an id value less than 5.
}
