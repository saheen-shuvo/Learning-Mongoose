{
    // Using $gt operator - greater than operator

    // db.test.find({id: {$gt: 5}})

    // Output:
    // By using the $gt operator, we can find documents where the id field is greater than 5. This will return all documents with an id value greater than 5.
}